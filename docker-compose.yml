services:
  frontend:
    build:
      context: ./stockfish-llm-chat   # adjust if your frontend folder has another name
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    working_dir: /app
    volumes:
      - ./stockfish-llm-chat:/app
    command: ["npm", "start"]
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    working_dir: /app
    volumes:
      - ./backend:/app
    command: ["npm", "start"]
